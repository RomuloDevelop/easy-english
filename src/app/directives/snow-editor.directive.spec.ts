import { SnowEditorDirective } from './snow-editor.directive';

describe('SnowEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new SnowEditorDirective();
    expect(directive).toBeTruthy();
  });
});
